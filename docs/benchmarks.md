# Benchmarks

Generated by `python tools/benchmark.py` on 2026-09-16T15:13.

## Environment

* Platform: Windows-10-10.0.26200-SP0 (AMD64), 12 CPUs
* Python 3.11.0; pandas 3.0.5, numpy 2.4.6, pyarrow 25.0.1
* R 4.5.2 (x86_64-w64-mingw32); arrow: yes
* Transport: stdio JSON + Arrow IPC files in a temp directory; repeats: 3 (median reported)

## Results

| Benchmark | Result |
|---|---|
| R worker startup | 0.55 s |
| scalar call r('1 + 1') | 7.8 ms median (6.7 ms best) |
| scalar assign | 0.4 ms median |
| DataFrame 1,000 rows (0.0 MB) via json | py->R 0.055 s, R->py 0.200 s, round trip lossless |
| DataFrame 1,000 rows (0.0 MB) via arrow | py->R 0.227 s, R->py 0.128 s, round trip lossless |
| DataFrame 100,000 rows (3.8 MB) via json | py->R 1.414 s, R->py 0.047 s, round trip lossless |
| DataFrame 100,000 rows (3.8 MB) via arrow | py->R 0.033 s, R->py 0.031 s, round trip lossless |
| DataFrame 1,000,000 rows (37.7 MB) via arrow | py->R 0.272 s, R->py 0.203 s, round trip lossless |
| numpy 1e6 doubles (Arrow) | py->R 0.022 s, R->py 0.022 s |
| proxy method call fit.coef() | 9.6 ms median |
| plot return (PNG) | 15 ms median |

## Method

Each timing wraps the public API call (`r.assign`, `r.get`, `r(...)`, proxy method) with `time.perf_counter()`; the median of the repeats is reported. 
Round-trip results are checked with `DataFrame.equals` after each transfer. No comparison with other tools is made here: 
embedded bridges (rpy2, reticulate) have lower per-call latency by design; RPython trades a few milliseconds per call for process isolation and symmetry. 
Re-run the script on your hardware before quoting numbers.
